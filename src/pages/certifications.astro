---
// Redirigir a la página de certificaciones en español
const redirectTo = '/es/certifications';

function redirectResponse() {
  return new Response(null, {
    status: 302,
    headers: {
      'Location': redirectTo
    }
  });
}

const response = redirectResponse();
export const GET = () => response;
---
