---
/**
 * Página de Contacto
 * Esta página muestra la información de contacto y un formulario para enviar mensajes
 */

import Layout from '@layouts/Layout.astro';
import Contact from '@components/Contact/Contact.astro';
import { textsJson } from '@utils/const';

// Usar español como idioma por defecto
const langJson = textsJson.es;
const title = langJson?.contactPage?.title || 'Contacto';
const description = langJson?.contactPage?.description || 'Formulario de contacto';
---

<Layout
  title={title}
  description={description}
  json={langJson}
>
  <Contact lang="es" json={langJson} />
</Layout>
