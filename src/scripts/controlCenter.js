/**
 * Controlador de funciones del Centro de Control 
 * Esta es una versión simple para resolver error de importación
 */

/**
 * Inicializa el Centro de Control
 */
export function initControlCenter() {
  console.log("Control Center initialized");
  // Implementación mínima para evitar errores
  return {
    isInitialized: true
  };
}
