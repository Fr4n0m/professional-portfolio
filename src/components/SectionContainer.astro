---
const { class: className, id } = Astro.props;
---

<section
	id={id}
	data-section={id}
	class={`section ${className} scroll-m-36 md:scroll-m-28 w-full mx-auto container lg:w-[740px] md:pb-24`}
>
	<slot />
</section>
