---
import { TAGS } from '@utils/tags';
import Tag from '@components/ui/Tag.astro';
---

<ul class='flex gap-x-3 flex-row text-nowrap flex-wrap -mt-3 md:-mt-0'>
	{
		Object.values(TAGS).map(tag => (
			<li>
				<Tag {...tag} />
			</li>
		))
	}
</ul>
