---
const { class: className = 'size-5' } = Astro.props;
---

<!-- SF Symbol display oficial de Apple (equivalente a 'computer' en SF Symbols) -->
<svg {...Astro.props} class={`system-icon ${className}`} viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
  <!-- Pantalla del ordenador -->
  <rect x="2" y="4" width="20" height="12" rx="2" stroke="currentColor" stroke-width="2" fill="none"/>
  
  <!-- Base del ordenador -->
  <path d="M8 19.5h8M12 16v3.5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
  
  <!-- Detalle de la pantalla (al estilo Apple) -->
  <rect x="4" y="6" width="16" height="8" fill="currentColor" opacity="0.12"/>
</svg>

<style>
  /* Estilo para reflejar mejor SF Symbols */
  .system-icon {
    stroke-width: 2.2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  
  /* Ajustes específicos para dispositivos Apple */
  @supports (-webkit-backdrop-filter: none) {
    .system-icon {
      stroke-width: 2.5;
    }
  }
</style>